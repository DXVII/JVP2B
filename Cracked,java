import org.newdawn.slick.Input;
import org.newdawn.slick.SlickException;
import org.newdawn.slick.Graphics;
import org.newdawn.slick.Image;

public class Cracked extends Wall {

    public Wall(String image_src, Position position) throws SlickException {
		super(image_src, postion);
    }

    public void render(Graphics g){
        super.render(g)
    }

    public void explode(){
        /* create new Explosion object and pass to world */
        /* explode ticks then explode derenders */
        /* unblock */
    }

    /*Method - unlock*/
        //roadBlock false
        // super.super(togRendered) false
}
